<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Apply for Coaching</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond&family=Montserrat&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@500&display=swap" rel="stylesheet">

    <style>
        body {
            background: #0f0d0b url('https://images.steamusercontent.com/ugc/1881968224931895469/0A495BF124C324A2E09685D782BCAA891E394FF6/?imw=1024&imh=570&ima=fit&impolicy=Letterbox&imcolor=%23000000&letterbox=true') no-repeat center center fixed;
            background-size: cover;
            color: #c4b997;
            font-family: 'Montserrat', sans-serif;
            min-height: 100vh;
            margin: 0;
            position: relative;
        }


        body::before {
            content: "";
            position: fixed;
            inset: 0;
            background: rgba(15, 13, 11, 0.9);
            z-index: 0;
        }


        .navbar {
            background-color: rgba(40, 33, 28, 0.95) !important;
            border-bottom: 1.5px solid #bfae7d;
            font-family: 'EB Garamond', serif;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            box-shadow: 0 2px 12px #bfae7d99;
            z-index: 1030;
            padding: 0.35rem 1rem;
            height: 52px;
        }

        .navbar-brand {
            color: #d6c79c !important;
            font-size: 1.35rem;
            line-height: 1.4;
            user-select: none;
        }

        .navbar-nav .nav-link {
            color: #c4b997 !important;
            font-weight: 500;
            font-size: 0.95rem;
            padding: 0.4rem 0.7rem;
            transition: color 0.25s ease, text-shadow 0.25s ease;
        }

        .navbar-nav .nav-link:hover,
        .navbar-nav .nav-link:focus {
            color: #fff2c1 !important;
            text-shadow: 0 0 7px #bfae7d;
            outline: none;
        }

        h1 {
            font-family: 'EB Garamond', serif;
            font-weight: 700;
            font-size: 2.5rem;
            text-align: center;
            margin: 80px 0 40px;
            color: #ffffff;
            user-select: none;
            letter-spacing: 0.07em;
            text-shadow: 0 0 8px #ffffffaa;
        }

        .game-list {
            max-width: 900px;
            margin: 0 auto 60px;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
            gap: 28px;
            padding: 0 15px;
            z-index: 1;
            position: relative;
        }

        .game-card {
            background: rgba(40, 33, 28, 0.85);
            border-radius: 14px;
            box-shadow:
                0 4px 10px rgba(0, 0, 0, 0.7),
                inset 0 0 15px #bfae7d66;
            text-align: center;
            padding: 15px 12px;
            user-select: none;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .game-card:hover {
            transform: translateY(-6px);
            box-shadow:
                0 8px 20px rgba(0, 0, 0, 0.9),
                inset 0 0 30px #fff2c188;
            cursor: pointer;
        }

        .game-name {
            color: #d6c79c;
            font-family: 'EB Garamond', serif;
            font-weight: 600;
            font-size: 1.1rem;
            text-decoration: none;
            display: block;
            margin-top: 12px;
            transition: color 0.3s ease, text-shadow 0.3s ease;
        }

        .game-name:hover,
        .game-name:focus {
            color: #fff2c1;
            text-shadow: 0 0 12px #bfae7d;
            outline: none;
        }

        .game-card img {
            width: 100%;
            height: 140px;
            object-fit: cover;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
            user-select: none;
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 2rem;
                margin-top: 70px;
            }

            .game-list {
                grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
                gap: 22px;
            }

            .game-card img {
                height: 120px;
            }
        }

        .site-footer {
            position: relative;
            bottom: 0;
            width: 100%;
            background-color: #1b1b1b;
            color: #e6d4a3;
            font-family: 'Cinzel', serif;
            padding: 25px 10px;
            text-align: center;
            border-top: 2px solid #a88e49;
            box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.7);
            margin-top: 60px;
        }

        .site-footer .footer-content p {
            margin: 8px 0;
            font-size: 16px;
            letter-spacing: 0.5px;
        }

        @media (max-width: 600px) {
            .site-footer .footer-content p {
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <%- include("../partials/navbar") %>

        <main>
            <h1>Apply for Coaching</h1>

            <div class="game-list">
                <% games.forEach(game=> { %>
                    <div class="game-card">
                        <a href="/service/<%= encodeURIComponent(game.name) %>" class="game-name" tabindex="0">
                            <img src="<%= game.image %>" alt="<%= game.name %>" />
                            <%= game.name %>
                        </a>
                    </div>
                    <% }) %>
            </div>
        </main>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <footer class="site-footer">
            <div class="footer-content">
                <p>Credit goes to: <strong>75%</strong> Israa, <strong>20%</strong> Omar, <strong>5%</strong> Goearge, <strong>Spirit wise</strong> Zaid
                </p>
                <p>Contact: +973 38776650</p>
                <p>Email: Yousifabdulaziz18@gmail.com</p>

            </div>
        </footer>
</body>

</html>