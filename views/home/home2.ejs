<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Apply for Coaching</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background: #f0f0f0;
        }

        h1 {
            text-align: center;
        }

        .game-list {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            margin-top: 30px;
        }

        .game-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            width: 180px;
            text-align: center;
            cursor: pointer;
            transition: transform 0.2s ease;
            border: 2px solid transparent;
        }

        .game-card:hover {
            transform: scale(1.05);
            border-color: #007BFF;
        }

        .game-card img {
            width: 100%;
            height: 140px;
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
            object-fit: contain;
            background: #f9f9f9;
            padding: 10px;
        }

        .game-name {
            padding: 10px;
            font-weight: bold;
            font-size: 1.1em;
        }

        form {
            margin: 0;
        }

        button.game-select-btn {
            background: none;
            border: none;
            padding: 0;
            width: 100%;
            font: inherit;
            cursor: pointer;
            outline: inherit;
        }
    </style>
</head>

<body>
    <h1>List of games</h1>
   <div class="game-list">
   <% games.forEach(game => { %>
        <form action="/" method="POST" class="game-card">
          <input type="hidden" name="gameName" value="<%= game.name %>" />
          <button class="game-select-btn" type="submit" title="Apply coaching for <%= game.name %>">
            <img src="<%= game.image %>" alt="<%= game.name %>" />
            <div class="game-name"><%= game.name %></div>
          </button>
        </form>
      <% }) %>
</div>

</body>

</html>