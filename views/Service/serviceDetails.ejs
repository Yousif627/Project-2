<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Coach Services</title>
</head>

<body>
  <h1>Coach Services</h1>

  <% if(user.role === "coach"){ %>
  <a href="/service/new/<%= gameName %>">Add New Service</a>
  <%} %>

  <ul>
    <% services.forEach(service=> { %>
      <li>
        <strong>Game:</strong>
        <%= service.gameName %><br />
          <strong>Experience:</strong>
          <%= service.yearsOfExperience %> years<br />
            <strong>Price:</strong> $<%= service.price %> per hour<br />
              <strong>Description:</strong>
              <%= service.description %><br />

              <% if(user && user._id == service.user._id) {%>
                  <!-- Edit button -->
                <a href="/service/<%= service._id %>/edit">Edit</a>

                <!-- Delete button -->
                <form action="/service/<%= service._id %>?_method=DELETE" method="POST" style="display:inline;"
                  onsubmit="return confirm('Are you sure you want to delete this service?');">
                  <button type="submit" style="background-color:red; color:white;">
                    Delete
                  </button>
                </form>
              <%} %>
              
      </li>
      <hr />
      <% }) %>
  </ul>
</body>

</html>