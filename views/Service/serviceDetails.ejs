<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Coach Services</title>

  </style>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@700&family=Merriweather&display=swap"
    rel="stylesheet" />

  <style>
    body {
      min-height: 100vh;
      margin: 0;
      background: url('https://i.redd.it/j7mhyctxmg771.gif') no-repeat center center fixed;
      background-size: cover;
      color: #d4c295;
      font-family: 'Merriweather', serif;
      position: relative;
      padding-bottom: 50px;
    }


    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(10, 8, 6, 0.85);
      z-index: 0;
    }

    .container {
      position: relative;
      z-index: 1;
      max-width: 900px;
    }

    .navbar {
      background: rgba(25, 22, 18, 0.9) !important;
      border-bottom: 3px solid #b2924f;
      box-shadow: 0 0 15px rgba(178, 146, 79, 0.8);
      margin-bottom: 30px;
      font-family: 'Cinzel', serif;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      font-weight: 700;
      z-index: 10;
    }

    .navbar-brand {
      color: #b2924f !important;
      font-size: 1.8rem;
      text-shadow: 0 0 4px #b2924f;
    }

    .navbar-nav .nav-link {
      color: #d4c295 !important;
      font-weight: 600;
      transition: color 0.3s ease;
    }

    .navbar-nav .nav-link:hover {
      color: #b2924f !important;
      text-shadow: 0 0 6px #b2924f;
    }


    .service-card {
      background: rgba(228, 215, 174, 0.9);
      color: #3b2f1a;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(178, 146, 79, 0.7);
      padding: 25px;
      margin-bottom: 30px;
      font-family: 'Merriweather', serif;
      border: 2px solid #b2924f;
      transition: box-shadow 0.3s ease;
    }

    .service-card:hover {
      box-shadow: 0 0 40px rgba(178, 146, 79, 1);
    }

    .service-info strong {
      color: #8c732f;
      font-family: 'Cinzel', serif;
      font-weight: 700;
      letter-spacing: 0.05em;
    }


    .btn-edit {
      background-color: #b2924f;
      color: #2e261b;
      border: 2px solid #7a6629;
      padding: 12px 28px;
      border-radius: 30px;
      font-family: 'Cinzel', serif;
      font-weight: 700;
      box-shadow: 0 0 20px rgba(178, 146, 79, 0.9);
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      margin-right: 12px;
      text-decoration: none;
      font-size: 1.2rem;
      text-shadow: 0 0 4px #fff9d1;
    }

    .btn-edit:hover {
      background-color: #7a6629;
      box-shadow: 0 0 30px rgba(178, 146, 79, 1);
      color: #f0e8a9;
    }

    .btn-delete {
      background-color: #5c3c1b;
      color: #f0e8c1;
      border: 2px solid #3d2b0f;
      padding: 8px 20px;
      border-radius: 30px;
      font-family: 'Cinzel', serif;
      font-weight: 700;
      box-shadow: 0 0 12px rgba(92, 60, 27, 0.8);
      cursor: pointer;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-delete:hover {
      background-color: #3d2b0f;
      box-shadow: 0 0 22px rgba(92, 60, 27, 1);
      color: #f0e8c1;
    }

    hr {
      border-color: #b2924f;
      opacity: 0.7;
      margin: 40px 0;
    }


    h1 {
      font-family: 'Cinzel', serif;
      font-weight: 900;
      font-size: 3rem;
      letter-spacing: 0.12em;
      margin-bottom: 40px;
      color: #b2924f;
      text-shadow: 0 0 6px #8c732f;
      text-align: center;
    }

    @media (max-width: 576px) {
      .service-card {
        padding: 15px;
      }

      .btn-edit,
      .btn-delete {
        padding: 6px 14px;
        font-size: 0.9rem;
      }
    }
  </style>
</head>

<body>
  <%- include("../partials/navbar") %>
   <div class="container">
    <h1 class="page-title">Coach Services</h1>

     <% if(user.role==="coach" ){ %>
     <div class="add-service-link">
       <a href="/service/new/<%= gameName %>" class="btn btn-add">+ Add New Service</a>
     </div>
       <% } %>

      <ul class="service-list">
      <% services.forEach(service=> { %>
         <li class="service-card">
            <p><strong>Game:</strong>
             <%= service.gameName %>
            </p>
            <p><strong>Experience:</strong>
            <%= service.yearsOfExperience %> years
            </p>
            <p><strong>Price:</strong> $<%= service.price %> per hour</p>
            <p><strong>Description:</strong>
            <%= service.description %>
            </p>

            <% if(user && user._id==service.user._id) { %>
            <div class="service-actions">
            <a href="/service/<%= service._id %>/edit" class="btn btn-edit">Edit</a>
            <form action="/service/<%= service._id %>?_method=DELETE" method="POST" class="delete-form"
             onsubmit="return confirm('Are you sure you want to delete this service?');">
            <button type="submit" class="btn btn-delete">Delete</button>
            </form>
            </div>
            <% } %>
            </li>
            <% }) %>
          </ul>
    </div>
</body>

</html>