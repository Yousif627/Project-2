<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Services</title>
</head>
<body>
  <h1>Your Services</h1>

<% userServices.forEach(service => { %>
    <p><strong>Game:</strong> <%= service.gameName %></p>
    <p><strong>Experience:</strong> <%= service.gameExperience %> years</p>
    <p><strong>Price:</strong> $<%= service.price %>/hr</p>
    <p><strong>Description:</strong> <%= service.description %></p>

    <a href="/service/<%= service._id %>/edit">Edit</a>

    <form action="/service/<%= service._id %>?_method=DELETE" method="post" onsubmit="return confirm('Are you sure?');" style="display:inline;">
      <button type="submit" style="background-color:red; color:white;">Delete</button>
    </form>
  </div>
<% }); %>

</body>
</html>
