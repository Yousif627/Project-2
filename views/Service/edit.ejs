<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Edit Coaching Service</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond&family=Montserrat:wght@400;600&display=swap"
        rel="stylesheet" />

    <style>
        body {
            background: #0f0d0b url('https://www.gifcen.com/wp-content/uploads/2024/03/elden-ring-gif.gif') no-repeat center center fixed;
            background-size: 100% 100%;
            color: #c4b997;
            font-family: 'Montserrat', sans-serif;
            min-height: 100vh;
            margin: 0;
            position: relative;
        }


        body::before {
            content: "";
            position: fixed;
            inset: 0;
            background: rgba(15, 13, 11, 0.9);
            z-index: 0;
        }

        .container {
            position: relative;
            z-index: 1;
            max-width: 480px;
            background: rgba(40, 33, 28, 0.9);
            border-radius: 15px;
            padding: 35px 40px;
            margin: 100px auto 60px;
            box-shadow:
                0 4px 15px rgba(0, 0, 0, 0.7),
                inset 0 0 30px #bfae7d66;
        }


        h1 {
            font-family: 'EB Garamond', serif;
            font-weight: 700;
            font-size: 2.2rem;
            text-align: center;
            margin-bottom: 32px;
            color: #d6c79c;
            user-select: none;
            letter-spacing: 0.07em;
            text-shadow: 0 0 8px #bfae7daa;
        }

        label {
            font-weight: 600;
            color: #bfae7d;
            margin-top: 22px;
            display: block;
            font-size: 1rem;
            user-select: none;
            text-shadow: 0 0 2px #6a5e3fcc;
        }

        input[type="number"],
        textarea {
            width: 100%;
            padding: 12px 18px;
            margin-top: 8px;
            border-radius: 10px;
            border: 2px solid #d6c79c88;
            background-color: #2b241f;
            color: #e7debb;
            font-size: 1.05rem;
            font-family: 'Montserrat', sans-serif;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            resize: vertical;
            box-shadow: inset 0 2px 5px #1a150f88;
        }

        input[type="number"]:focus,
        textarea:focus {
            border-color: #fff2c1;
            outline: none;
            box-shadow:
                0 0 12px #fff2c1bb,
                inset 0 2px 8px #3e2f16bb;
            background-color: #3c3223;
        }

        button {
            margin-top: 36px;
            width: 100%;
            padding: 14px 0;
            background-color: #bfae7d;
            border: none;
            border-radius: 30px;
            font-family: 'EB Garamond', serif;
            font-weight: 700;
            font-size: 1.3rem;
            color: #2b241f;
            cursor: pointer;
            box-shadow: 0 0 18px #bfae7dcc;
            transition: background-color 0.35s ease, box-shadow 0.35s ease;
            user-select: none;
        }

        button:hover,
        button:focus {
            background-color: #fff2c1;
            box-shadow: 0 0 28px #fff2c1dd;
            outline: none;
        }

        a.back-link {
            display: block;
            margin-top: 24px;
            color: #bfae7d;
            font-weight: 600;
            text-align: center;
            text-decoration: none;
            user-select: none;
            transition: color 0.3s ease, text-shadow 0.3s ease;
        }

        a.back-link:hover,
        a.back-link:focus {
            color: #fff2c1;
            text-shadow: 0 0 9px #bfae7d;
            outline: none;
        }

        @media (max-width: 480px) {
            .container {
                padding: 30px 25px;
                margin: 90px auto 50px;
            }

            h1 {
                font-size: 1.9rem;
            }

            button {
                font-size: 1.1rem;
                padding: 13px 0;
            }
        }
    </style>
</head>

<body>

    <main class="container" role="main">
        <h1>Edit Service</h1>

        <form action="/service/<%= services._id %>?_method=PUT" method="post" novalidate>
            <input type="hidden" name="gameName" value="<%= services.gameName %>" />

            <label for="yearsOfExperience">Years of Experience:</label>
            <input type="number" id="yearsOfExperience" name="yearsOfExperience"
                value="<%= services.yearsOfExperience %>" min="0" required />

            <label for="price">Price per hour ($):</label>
            <input type="number" id="price" name="price" value="<%= services.price %>" min="0" step="0.01" required />

            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required><%= services.description %></textarea>

            <button type="submit">Update Service</button>
        </form>

        <a href="/service/<%= services.gameName %>" class="back-link">← Back to Services</a>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>