<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Apply to Coach </title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@600&family=Open+Sans&display=swap"
        rel="stylesheet" />

    <style>
        body {
            background: #12100f url('https://www.icegif.com/wp-content/uploads/2023/12/icegif-50.gif') no-repeat center center fixed;
            background-size: cover;
            color: #f3e5ab;
            font-family: 'Open Sans', sans-serif;
            min-height: 100vh;
            padding-top: 70px;
            position: relative;
            margin: 0;
        }

        body::before {
            content: "";
            position: fixed;
            inset: 0;
            background: rgba(18, 16, 15, 0.85);
            z-index: 0;
        }

        .container {
            position: relative;
            z-index: 1;
            max-width: 480px;
            background: #1f1b18dd;
            border-radius: 12px;
            padding: 30px 35px;
            box-shadow: 0 0 20px #f3e5abaa;
            margin: auto;
        }



        h2 {
            font-family: 'Cinzel', serif;
            font-weight: 700;
            font-size: 2rem;
            text-align: center;
            margin-bottom: 30px;
            color: #f3e5ab;
            user-select: none;
            letter-spacing: 0.06em;
        }

        label {
            font-weight: 600;
            color: #d6c79c;
            margin-top: 20px;
            display: block;
            font-size: 1rem;
            user-select: none;
        }

        input[type="number"],
        textarea {
            width: 100%;
            padding: 10px 15px;
            margin-top: 6px;
            border-radius: 8px;
            border: 1.5px solid #d6c79c;
            background-color: #2f2a25;
            color: #f3e5ab;
            font-size: 1rem;
            font-family: 'Open Sans', sans-serif;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            resize: vertical;
        }

        input[type="number"]:focus,
        textarea:focus {
            border-color: #f3e5ab;
            outline: none;
            box-shadow: 0 0 12px #f3e5abaa;
            background-color: #3a342f;
        }

        button {
            margin-top: 32px;
            width: 100%;
            padding: 12px 0;
            background-color: #f3e5ab;
            border: none;
            border-radius: 25px;
            font-family: 'Cinzel', serif;
            font-weight: 700;
            font-size: 1.25rem;
            color: #1f1b18;
            cursor: pointer;
            box-shadow: 0 0 15px #f3e5aba0;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
            user-select: none;
        }

        button:hover {
            background-color: #d6c79c;
            box-shadow: 0 0 25px #f3e5abcc;
        }

        @media (max-width: 480px) {
            .container {
                padding: 25px 20px;
            }

            h2 {
                font-size: 1.75rem;
            }

            button {
                font-size: 1.1rem;
                padding: 11px 0;
            }
        }

        .back-home-link {
            margin-top: 10px;
        }

        .home-link {
            display: inline-block;
            padding: 8px 16px;
            background-color: rgba(40, 40, 40, 0.7);
            color: #f0e6d2;
            border: 1px solid #a39274;
            border-radius: 5px;
            text-decoration: none;
            font-family: 'Cinzel', serif;
            transition: all 0.3s ease;
        }

        .home-link:hover {
            background-color: #a39274;
            color: #1c1c1c;
            text-decoration: none;
            transform: scale(1.05);
        }
    </style>
</head>

<body>

    <main class="container" role="main">
        <h2>Apply to Coach for <%= gameName %>
        </h2>

        <form action="/service" method="POST" novalidate>
            <input type="hidden" name="gameName" value="<%= gameName %>" />

            <label for="yearsOfExperience">Years Of Experience</label>
            <input type="number" id="yearsOfExperience" name="yearsOfExperience" required min="0" />

            <label for="description">Description</label>
            <textarea id="description" name="description" required rows="5"></textarea>

            <label for="price">Price per Session ($)</label>
            <input type="number" id="price" name="price" required min="0" step="0.01" />

            <button type="submit">Submit Coaching Service</button>
        </form>
        <div class="text-center">
            <a href="/" class="text-light text-decoration-underline">← Back to Home</a>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>